<form method="post" action="/admin/password">
    <input type="text" name="username" placeholder="username" value="{{userModel.username}}" required >
    <br> <br>
    <input type="password" name="password" placeholder="password" value="{{userModel.password}}" required >
    <br> <br>
    <input type="text" name="role" placeholder="role" value="{{userModel.role}}" required >
    <br> <br>
    <input type="hidden" name="guidId" value={{id}} >
    <input type="submit" value="Edit et" >
</form>
<br> 
